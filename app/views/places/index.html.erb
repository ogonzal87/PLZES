<h2>This is a list of all the cool bars in <%#= @places.search(location) %>.</h2>

<ul>
	<% @places.each do |place| %>
		<li>
			<%= place['name'] %>
			<%= link_to "Add to My Places", myplaces_path(place: { name: place['name'] }), 
        method: :post %>
    </li>
	<% end %>
</ul>


